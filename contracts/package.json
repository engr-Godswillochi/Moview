{
    "name": "moview-contracts",
    "version": "1.0.0",
    "description": "Smart contracts for Moview - Onchain Movie Review Platform",
    "scripts": {
        "compile": "hardhat compile",
        "test": "hardhat test",
        "deploy": "hardhat run scripts/deploy.ts --network celoSepolia",
        "verify": "hardhat verify --network celoSepolia"
    },
    "devDependencies": {
        "@nomicfoundation/hardhat-toolbox": "^5.0.0",
        "@nomicfoundation/hardhat-verify": "^2.0.0",
        "hardhat": "^2.22.0",
        "@types/node": "^22.0.0",
        "typescript": "^5.6.0"
    },
    "dependencies": {
        "@openzeppelin/contracts": "^5.0.0",
        "dotenv": "^16.4.0"
    }
}